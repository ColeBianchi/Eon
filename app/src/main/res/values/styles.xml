<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:textColorPrimary">@color/icons</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>
        <item name="colorButtonNormal">@color/primary</item>
        <item name="dividerVertical">@color/divider</item>
    </style>

    <style name="SCBSwitch" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/scb_primary</item>
        <item name="colorPrimaryDark">@color/scb_primary_dark</item>
        <item name="colorAccent">@color/scb_accent</item>
        <item name="colorControlActivated">@color/scb_enable</item>
    </style>
</resources>
